@model IEnumerable<ProjetoASIp1.Domain.Entities.Fornecedor>

@{
    ViewBag.Title = "ObtemFornecedor";
}

<h2>ObtemFornecedor</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Preco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipoDeServico)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Preco)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipoDeServico)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cnpj }) |
            @Html.ActionLink("Details", "Details", new { id=item.cnpj }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cnpj })
        </td>
    </tr>
}

</table>
